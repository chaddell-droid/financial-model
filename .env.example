# Copy to .env (gitignored) and fill in. Never commit real keys.
# Alpha Vantage API key for the optional MSFT live-price fallback in
# MsftVestingChart ("Live" button). The keyless Yahoo endpoint is primary;
# when this is unset the Alpha Vantage fallback is simply skipped.
VITE_ALPHA_VANTAGE_KEY=
